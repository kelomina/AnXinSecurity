# 启动快照扫描可信环境增强 - 收敛版评估与修改计划

## 一、总体结论

这份建议的方向是贴合项目实际的：它围绕启动快照、签名验证、进程空洞化、拦截队列继续加固，和 AnXinSecurity 当前已经存在的安全边界一致。可以把启动快照理解成“开门营业前的全店巡检”：应用启动后先看一遍当前系统里正在运行的进程和模块，判断有没有明显异常，再决定是否提醒用户或进入拦截队列。现在项目已经有文件扫描、签名验证、主映像完整性检测、拦截队列和快照统计，所以继续在这条链路上补“系统关键进程是否被冒充”“签名证书是否被吊销”“未来是否具备远程线程注入检测的基础”是合理的。

但原文需要收敛。部分建议把第一版实现做得过重，例如直接新增多个规则文件、单独新增证书吊销事件、在启动阶段加入远程线程检测、引入复杂风险评分和 Windows 官方 hash 交叉校验。这些想法不是没有价值，而是会让当前阶段的开发成本、误报风险和维护成本显著上升。当前更稳的做法是：P0 阶段只补关键、轻量、可验证的安全缺口；重检测和复杂规则系统放到后续阶段。

特别需要保留的一点是 P0.3 的缓存键风险。当前 `TrustService` 的签名缓存按路径缓存 `trusted/status`，如果未来新增“检查证书吊销”和“不检查证书吊销”两种模式，却仍然共用同一个缓存键，就可能出现“不检查吊销时得到的可信结果”污染“需要检查吊销的查询”。这不是文档层面的洁癖，而是真实安全漏洞：攻击者可能利用缓存让更严格的检查拿到更宽松的旧结果。

这个概念可以类比成门卫查证件：普通访客只查身份证是否像本人，重点访客还要查证件是否被注销。如果门卫只按“姓名”记缓存，下次重点访客来了，系统直接复用“上次身份证像本人”的结果，就漏掉了“是否被注销”这个更严格的检查。因此缓存键必须把“是否检查吊销”也记进去。

这个概念我解释清楚了吗？有没有哪里觉得绕？

---

## 二、保留但收敛的建议

| 模块 | 当前判断 | 收敛后的优先级 | 处理方式 |
|------|----------|----------------|----------|
| P0.1 进程伪装检测 | 贴合项目，成本低 | 第一阶段 | 只做关键系统进程名称 + 规范化路径校验 |
| P0.3 证书吊销检查 | 方向正确，但不能阻塞启动 | 第二阶段 | 快照快检，后台异步吊销检查，缓存键区分检查模式 |
| P0.2 远程线程注入检测 | 安全价值高，但启动成本高 | 后续阶段 | P0 只准备模块地址复用，不启用检测 |
| 风险聚合 | 方向可取，但第一版不落地评分表 | 后续阶段 | 先合并原因，复用现有 PID 去重队列 |
| 可信基线缓存防污染 | 思路重要，但原方案过重 | 后续阶段 | 删除官方 hash 交叉校验描述，保留 DPAPI/完整性方向 |

---

## 三、P0.1 进程伪装检测

### 3.1 要做什么

第一阶段只做“系统关键进程路径是否合理”的轻量检查。系统里有一些关键进程名称非常敏感，比如 `svchost.exe`、`csrss.exe`、`lsass.exe`、`winlogon.exe`、`services.exe`、`explorer.exe`。正常情况下，这些进程应该出现在 Windows 的固定目录里。如果某个 `lsass.exe` 出现在用户下载目录、临时目录或其它奇怪位置，就很可能是恶意程序借用了系统进程的名字。

这就像有人穿着“保安”制服进公司，但他不是从保安室来的，而是从停车场角落钻出来的。制服名称相同不能直接信任，还要看他是不是来自合理的位置。

### 3.2 为什么这样选

这个方案性能代价很低，只需要做进程名匹配和路径比较，不需要联网，不需要扫描整文件，也不需要枚举线程。它适合放在启动快照里，且应该在签名验证之前执行。原因是它可以快速发现明显伪装的关键进程，命中后直接进入现有拦截队列，减少后续签名验证和引擎扫描的浪费。

### 3.3 收敛后的实现边界

- 使用静态内置默认列表，或放入 `config/app.json` 的只读静态配置段。
- 第一版不新增 `config/process_whitelist.json`。
- 如果未来允许用户编辑关键进程名单，必须走 APPDATA + DPAPI 的运行时存储，不能写回仓库配置。
- 第一版不做莱文斯坦编辑距离、相似名称评分和复杂风险评分。
- 检测结果进入现有 `process-intercepted` 拦截链路，不新增平行弹窗事件。

### 3.4 路径规范化要求

路径比较前必须规范化，否则攻击者可能利用 Windows 路径特性绕过检查：

1. 统一路径分隔符和大小写。
2. 剥离 `\\?\`、`\??\` 等前缀。
3. 尽量解析 8.3 短文件名，例如 `C:\PROGRA~1`。
4. 尽量解析符号链接和 Junction Point。
5. 保留 `System32`、`SysWOW64`、Windows 根目录等合法位置的区分。

### 3.5 接入点

- 在 `src-tauri/src/services/snapshot_service.rs` 的 `take_startup_snapshot` 进程循环中，签名验证前执行。
- 伪装检测不受 `path_policy.should_skip_security_scan` 控制，因为它不是文件扫描，而是启动安全基线检查。
- 命中路径异常时，写入现有 `InterceptionService`。
- `SnapshotResult` 增加 `masqueradeAlerts`，沿用现有 `imageIntegrityAlerts`、`unknownProcesses`、`unknownModules` 的统计风格。

### 3.6 第一版返回值

```rust
pub enum MasqueradeVerdict {
    /// 不属于系统关键进程名，不需要检查
    NotApplicable,
    /// 系统关键进程路径符合预期
    Clean,
    /// 系统关键进程名称命中，但路径不符合预期
    Suspicious { expected: Vec<String>, actual: String },
}
```

---

## 四、P0.3 证书吊销检查

### 4.1 要做什么

证书吊销检查用于确认“这个签名证书现在是否还有效”。一个文件有数字签名，只能说明它曾经由某个证书签过名；但如果这个证书后来被吊销，就像一张已经挂失的门禁卡，卡面看起来仍然是真的，但不能再作为可信凭证。

### 4.2 为什么不能直接放进启动快照

完整吊销检查通常涉及 CRL 或 OCSP 网络请求。CRL 可以理解成“证书黑名单文件”，OCSP 可以理解成“在线问发证机构这张证现在还算不算数”。这些操作可能很慢，也可能在离线环境失败。如果把它们直接放进启动快照，就会把启动巡检拖慢，甚至让用户感觉应用卡住。

因此第一版应采用两阶段策略：

1. 启动快照阶段继续做快速签名验证，不发起网络请求。
2. 启动完成后，对高风险或关键进程在后台做完整吊销检查。

### 4.3 缓存键必须修正

新增吊销检查前，必须先处理缓存键。`TrustService` 不能继续只按 `normalized_path` 缓存签名结果。缓存键必须至少包含：

```text
(normalized_path, check_revocation)
```

或者在缓存条目里明确保存 `revocation_checked`，并保证：

- 不检查吊销得到的可信结果，不能用于检查吊销的查询。
- 检查吊销失败或未知的结果，不能伪装成普通签名可信。
- 文件写入时间变化后，所有模式下的缓存都应失效。

### 4.4 接口与事件策略

第一版不新增 `certificate-revoked` 和 `certificate-revocation-unknown` 事件。原因是项目已经有 `process-intercepted` 拦截事件和 `snapshot-result` 统计结果，新增平行事件会增加前端状态管理、i18n 文案和测试成本。

收敛后的策略：

- 确认吊销时，复用现有 `process-intercepted` 进入拦截队列。
- 普通未知状态只记录日志和统计，不弹窗。
- 系统关键进程吊销状态未知时，不直接阻断用户，但在 `snapshot-result` 中统计为 `revocationUnknownCritical`，后续 UI 再统一展示。

### 4.5 `SnapshotResult` 字段

统一使用以下字段，不同时维护两套口径：

```rust
pub struct SnapshotResult {
    // ... 现有字段 ...

    #[serde(rename = "masqueradeAlerts")]
    pub masquerade_alerts: u32,

    #[serde(rename = "revocationAlerts")]
    pub revocation_alerts: u32,

    #[serde(rename = "revocationUnknownCritical")]
    pub revocation_unknown_critical: u32,
}
```

### 4.6 本地缓存策略

如果后续增加吊销结果持久化缓存，应放在 APPDATA 运行时目录，并复用项目已有 DPAPI 保护思路，不写入仓库 `config/`。缓存内容可以是证书指纹、吊销状态、检查时间和过期时间。过期后必须重新检查，不能把过期结果当作有效可信结果。

---

## 五、P0.2 远程线程注入检测

### 5.1 当前结论

远程线程注入检测有安全价值，但不适合作为当前 P0 启动路径的核心改动。它需要枚举线程、读取线程起始地址，并和进程模块地址范围做比较。这个过程系统调用多，权限失败场景多，误报也多。对于普通用户来说，它可能把启动巡检从“稍等一下”变成“明显卡住”。

所以第一版不启用远程线程检测。当前只做一个准备动作：复用已经存在的模块地址枚举能力。

### 5.2 保留的准备工作

当前 `process_scanner_service.rs` 已经有 `ProcessModuleInfo`，包含：

- `path`
- `base_address`
- `image_size`

同时也已有 `enumerate_process_module_info`，但它仍是私有函数。后续可以把它提升为 `pub(crate)`，让 `snapshot_service.rs` 在需要时直接拿到模块路径和模块地址范围，避免重复枚举模块。

### 5.3 延后内容

以下内容从当前 P0 文档中删除或标记为后续研究，不作为第一版实现要求：

- `security_vendor_whitelist.json`
- `dotnet_process_whitelist.json`
- 浏览器 renderer / GPU / utility 子进程排除规则
- `RemoteThreadDetector` 完整服务
- `GetThreadInformation` / `NtQueryInformationThread` 双 API fallback
- 远程线程检测对应的 `injectionAlerts` 快照字段

这些内容不是永远不做，而是需要等并发扫描、规则配置、误报治理和 UI 展示策略更成熟后再做。

---

## 六、风险聚合与拦截队列

当前阶段不落地复杂风险评分函数。原文中的评分表可以作为后续设计方向，但不应成为第一版实现要求。

原因是现有 `InterceptionService` 已经按 PID 做去重，第一版更重要的是把每个检测来源的原因写清楚，并在同一个进程触发多个检测时尽量合并 `reason` 和 `payload`，避免用户看到重复弹窗。

第一版策略：

- 高风险伪装进程：直接入现有拦截队列。
- 吊销确认：直接入现有拦截队列。
- 吊销未知：不入队，统计并记录日志。
- 多个检测命中同一 PID：优先保留最高风险原因，后续再做完整聚合。

---

## 七、可信基线缓存污染防护

原文关于可信基线污染的担忧是合理的，但“通过 Windows Update 官方 hash、Microsoft Safety Scanner 或 Windows Defender Offline hash 数据库交叉校验”的描述应删除。当前项目没有这条现成入口，把它写成实施要求会误导后续开发者，以为这是可以直接接入的能力。

收敛后的表达：

- P0 阶段不实现可信基线持久化缓存。
- 后续如果实现可信基线缓存，必须放在 APPDATA 运行时目录。
- 基线文件必须使用 DPAPI 或同等级本机保护机制。
- Unknown、扫描失败、签名验证超时、吊销状态未知的对象不能进入可信基线。
- Windows 版本变化、文件写入时间变化或签名状态变化时，基线必须失效或重新验证。

---

## 八、前端与 i18n 要求

所有用户可见文字必须走 i18n。第一版如果新增前端展示内容，只能新增语言包 key：

- `config/i18n/zh-CN.json`
- `config/i18n/en-US.json`

不要在 React 组件或 Rust 返回给前端的用户文案里硬编码新增中英文。Rust 侧可以返回结构化 reason code，前端负责根据 i18n key 展示用户可读文案。

第一版不新增平行事件，优先复用：

- `process-intercepted`
- `snapshot-result`
- 现有拦截弹窗和快照展示入口

---

## 九、改稿后的实施步骤

### 第一步：P0.1 进程伪装检测

- 在 `snapshot_service.rs` 的进程循环中，签名验证前执行关键系统进程路径校验。
- 命中路径异常时复用现有 `InterceptionService`。
- `SnapshotResult` 增加 `masqueradeAlerts`。
- 测试覆盖：合法系统路径、大小写差异、前缀剥离、异常路径入队、非关键进程不触发。

### 第二步：P0.3 签名吊销检查基础

- 在 `trust_service.rs` 增加区分吊销模式的验证入口。
- 缓存键纳入 `check_revocation`。
- 保持默认 `verify_file` 的兼容行为，不破坏现有调用方。
- 后台吊销检查结果复用现有拦截和快照统计，不新增独立事件。
- 测试覆盖：普通签名缓存与吊销签名缓存互不污染。

### 第三步：P0.2 准备工作

- 将 `enumerate_process_module_info` 从私有函数提升为 `pub(crate)`。
- 保持 `enumerate_process_modules` 的现有行为不变。
- 增加测试，证明路径列表调用方不受影响，模块地址信息可供后续检测复用。
- 不启用远程线程检测。

### 第四步：前端与文案

- 如需展示新增统计，补充 `zh-CN` 与 `en-US` i18n key。
- 前端只读取结构化字段，不解析 Rust 拼接的长句。
- 不新增新的 Tauri command、插件或 capability，除非后续代码实现确实需要。

### 第五步：验证

优先窄测试，避免一上来跑宽泛全量导致耗时过长：

```powershell
cargo check --manifest-path src-tauri/Cargo.toml
cargo test --manifest-path src-tauri/Cargo.toml process_masquerade --lib
cargo test --manifest-path src-tauri/Cargo.toml trust_service --lib
npm run test
```

如果后续实际改到前端类型或组件，再补：

```powershell
npm run typecheck
npm run lint
```

---

## 十、最终建议

当前 `建议.md` 应从“大而全的安全路线图”改成“P0 收敛实施说明”。最关键的取舍是：先做轻量、确定、能接入现有拦截链路的检测；把重型、易误报、需要大量配置治理的检测放到后续阶段。

第一版成功标准不是“把所有检测都做完”，而是：

1. 系统关键进程伪装能被快速发现。
2. 证书吊销检查不会污染现有签名缓存。
3. Unknown 不会进入可信基线。
4. 不新增不必要的前端事件和配置文件。
5. 后续远程线程检测有可复用的模块地址基础，但不会拖慢当前启动快照。
