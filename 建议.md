# 启动快照扫描可信环境增强 - 剩余工作清单

> 最后更新：2026-07-27

## 一、文档定位

这份文档只记录"还没做完"的内容。已完成项已标记并归档。

当前保留的内容分成两类：

- 未实现：还没有正式落地，只能作为后续工作
- 明确延后：不进入当前第一版

---

## 二、已完成项（归档）

| 模块 | 完成时间 | 验证方式 |
|------|----------|----------|
| P0.3 证书吊销检查 | 2026-07 | `trust_service.rs` 已实现 `verify_file_with_revocation()`，缓存键隔离（`|revocation=chain` / `|revocation=none`），单元测试覆盖 |
| P0.2 远程线程注入检测 | 2026-07 | `etw_service.rs` 实现 `apply_remote_thread_start_detection()`；`hook_service.rs` 实现完整注入链检测（OpenProcess→VirtualAllocEx→WriteProcessMemory→CreateRemoteThread）；`interception_service.rs` 以优先级 100 处理 |
| 前端与 i18n | 2026-07 | 所有新功能（防火墙、权限等）均已走 i18n，`zh-CN.json` 和 `en-US.json` 同步维护 |

---

## 三、当前保留项

| 模块 | 当前状态 | 说明 |
|------|----------|------|
| 风险聚合 | 未实现 | 暂不做复杂评分表，只保留同一 PID 的原因合并思路 |
| 可信基线缓存防污染 | 未实现 | 不做独立基线缓存，避免 Unknown、超时和失败结果污染可信状态 |

---

## 四、风险聚合与拦截队列

### 4.1 现在做到哪里

现有拦截链路已经能按 PID 去重，也能保留原因和负载。`risk_service.rs` 实现了单事件风险评级（severity → low/medium/high），但没有多事件聚合或复合评分。

### 4.2 还缺什么

- 不做多维风险分数表
- 不做独立风险引擎
- 不做一套和现有拦截链路平行的新队列

### 4.3 当前策略

- 高风险确认项直接复用现有拦截队列
- 普通 Unknown 只记录和统计，不直接入队
- 同一 PID 多次命中时，优先保留最强原因

---

## 五、可信基线缓存防污染

### 5.1 还没做完的部分

这部分现在只停留在原则层，没有做成独立的可信基线持久化缓存。

### 5.2 需要保持的边界

- 不引入独立 baseline cache
- 不让 Unknown、扫描失败、签名超时、吊销未知进入可信基线
- 如果未来真的要做缓存，必须放在 APPDATA，并使用本机保护机制
- 不做 Windows 官方 hash 交叉校验这类过重方案

---

## 六、建议的后续顺序

1. 确认风险聚合和可信基线缓存都不引入平行体系。
2. 如果决定实现，优先做同一 PID 原因合并（最小方案）。
3. 可信基线防污染只需确保现有缓存逻辑不把失败结果写入即可。

---

## 七、验证口径

后续如果真的要动这份清单里的内容，优先窄测试：

```powershell
cargo check --manifest-path src-tauri/Cargo.toml
cargo test --manifest-path src-tauri/Cargo.toml risk_service --lib
cargo test --manifest-path src-tauri/Cargo.toml trust_service --lib
npm run typecheck
npm run test
```
