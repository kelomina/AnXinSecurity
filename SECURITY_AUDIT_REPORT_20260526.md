# AnXinSecurity 安全审计报告

**审计日期**: 2026-05-26
**审计范围**: Tauri 应用 (Rust 后端 + React 前端)
**审计目标**: 识别中等严重度及以上的已确认漏洞，且必须具备可演示的端到端利用路径

---

## 审计摘要

经过系统性审计，本代码库**未发现中等或更高严重度的已确认漏洞**。

本报告识别了若干低风险发现和架构层面的安全考虑，但不构成可直接利用的漏洞。所有发现均已包含缓解措施或适用条件限制。

---

## 架构概述

| 组件 | 技术栈 | 信任边界 |
|------|--------|----------|
| 后端 | Rust + Tauri 2.0 | 进程内通信，通过 IPC 调用 |
| 前端 | React + TypeScript | 用户界面，无直接系统访问 |
| 存储 | SQLite + DPAPI 加密 | 运行时列表受 DPAPI 保护 |
| 扫描引擎 | 原生 Axon DLL | 外部引擎，进程隔离 |

**关键信任边界**：
- Tauri 命令层（Rust）是唯一可操作系统资源的入口
- 配置文件存储在 APPDATA，使用 DPAPI 加密运行时数据
- 进程注入和监控是应用核心功能，需高权限运行

---

## 已审计攻击面

### 1. 认证与访问控制

| 命令 | 保护机制 | 评估 |
|------|----------|------|
| `suspend_process` | 白名单保护 + 自保护检查 | ✅ 安全 |
| `terminate_process` | 白名单保护 + 自保护检查 | ✅ 安全 |
| `dev_settings_unlock` | 密码验证（存在逻辑缺陷，见下方） | ⚠️ 需注意 |

**发现 #1**: 开发者设置密码验证存在字段不匹配缺陷

**位置**: `src-tauri/src/commands/dev_settings.rs`

**详情**:
- `dev_settings_save` 将密码哈希写入 `passwordHash` 字段（全 64 字符 SHA-256 hex）
- `dev_settings_unlock` 从 `salt` 字段读取验证（仅为哈希前 32 字符）
- 实际影响：密码验证功能在当前实现下会**永久失败**

**利用路径**: 无（此缺陷导致功能不可用，而非安全绕过）

**严重度**: 不适用（功能缺陷，非安全漏洞）

---

### 2. 注入向量

#### SQL 注入
**评估**: ✅ **未发现**  
所有数据库操作使用 sqlx 参数化查询（`.bind()`），无原始 SQL 拼接。

#### Shell 命令注入
**评估**: ✅ **未发现**  
- `system.rs` 使用 `Command::new("wmic")` 硬编码参数，无用户输入拼接
- 进程注入器路径由配置提供，不经过用户输入

#### 文件路径操作
**评估**: ✅ **已安全处理**  
- ETW 事件中的文件路径经过 `normalize_path()` 标准化
- 目录遍历使用 `canonicalize()` 检测循环
- 深度保护限制为 32 层

---

### 3. 外部交互

#### ETW 事件处理
**评估**: ✅ **已安全处理**  
- ETW 事件解析后使用 serde 反序列化验证
- 事件数据不直接用于文件操作

#### 命名管道通信
**评估**: ✅ **已安全处理**  
- Hook 服务接收的 DLL 事件经过 JSON 解析验证
- 管道名称格式化为 `\\.\pipe\{}`，无注入风险

---

### 4. 敏感数据处理

#### 运行时列表加密
**评估**: ✅ **良好实践**  
- 排除项和允许列表使用 Windows DPAPI 加密
- 加密后存储在 `%APPDATA%\AnXinSecurity\runtime\`
- 支持从明文旧配置迁移

**发现 #2**: 隔离区服务使用硬编码回退密钥

**位置**: `src-tauri/src/services/quarantine_service.rs:130`

```rust
// 安全警告: 回退密钥仅用于开发/演示 — 生产环境请设置环境变量
*eprintln!("[QuarantineService] WARNING: 使用硬编码回退加密密钥...");
*b"AnXinSecurityKey"
```

**利用条件**: 仅当 `ANXIN_SECURITY_QUARANTINE_KEY` 环境变量未设置时触发

**缓解措施**: 
- 已在代码中明确警告生产环境必须设置环境变量
- 日志输出可见警告信息

**严重度**: 低（需物理访问或环境配置错误）

---

### 5. 进程控制风险

**发现 #3**: 进程控制命令缺乏认证层

**位置**: `src-tauri/src/commands/process.rs`

**详情**:
- `suspend_process`, `resume_process`, `terminate_process` 等命令可直接从前端调用
- 虽然存在系统进程保护（csrss.exe, smss.exe, wininit.exe, services.exe, lsass.exe），但无法防止终止其他任意进程

**攻击者画像**: 本地用户（需运行应用程序）

**影响**: 可终止用户有权限操作的任意进程

**评估**: 这是安全工具的**设计决策**，而非漏洞。应用程序本身需要终止进程的能力以实现其安全功能。真正的保护来自于：
1. 应用程序需要高权限运行
2. 系统进程受白名单保护

**严重度**: 低（设计固有风险）

---

### 6. DLL 注入风险

**发现 #4**: 进程监控可加载任意 DLL

**位置**: `src-tauri/src/services/process_monitor_service.rs:485`

```rust
std::process::Command::new(injector)
    .args(["--pid", &pid.to_string(), "--dll", dll])
    .spawn();
```

**详情**:
- `start_process_watcher` 命令接收注入器和 DLL 路径
- 对未签名进程执行 DLL 注入

**攻击者画像**: 需要能修改前端配置或应用启动参数

**评估**: 这是安全监控的核心功能。恶意利用需要：
1. 修改应用程序配置或启动参数
2. 在运行时替换 DLL 文件

**严重度**: 低（需先获取应用程序控制权）

---

## 安全实践评估

| 实践 | 状态 |
|------|------|
| 参数化 SQL 查询 | ✅ 良好 |
| DPAPI 加密敏感数据 | ✅ 良好 |
| 进程保护白名单 | ✅ 良好 |
| 路径规范化 | ✅ 良好 |
| 循环检测（junction/symlink） | ✅ 良好 |
| 深度限制 | ✅ 良好 |
| 错误处理不泄露敏感信息 | ✅ 良好 |
| 敏感数据日志记录 | ✅ 未发现 |

---

## 建议（非必需）

以下为可选的安全增强建议：

1. **开发者设置功能修复**: 统一 save 和 unlock 使用的字段名称
2. **进程控制审计日志**: 增加对终止/挂起操作的审计记录
3. **DLL 路径验证**: 验证 DLL 文件签名或路径在预期目录内
4. **强制环境变量检查**: 启动时检查 `ANXIN_SECURITY_QUARANTINE_KEY` 是否设置

---

## 结论

**审计完成——未发现中等或更高严重度的已确认漏洞。**

代码库整体安全架构设计合理，遵循了 Rust 的内存安全特性，并正确使用了 Windows 安全 API（DPAPI、WinVerifyTrust）。主要风险点来自：
1. 应用程序作为安全工具的固有设计（可终止进程、可注入 DLL）
2. 开发/演示环境的配置要求（需设置环境变量）

这些风险在实际部署中可控，建议在生产环境部署清单中明确环境变量配置要求。
